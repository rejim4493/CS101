#include <stdio.h>
#include <stdlib.h>

int main()
{
    int W = 1;
    int H = 1;
    int N = 1;
    bool broad[500][500] = {};
    while(scanf("%d%d%d", &W, &H, &N) == 3&&W!=0&&H!=0&&N!=0){//吃進棋盤長寬和矩形數量
        for(int i = 0; i<500;i++){
            for(int j = 0; j<500;j++){
                broad[i][j] = false;
            }
        }//先將棋盤列為空的(false)
        for(int i = 0;i < N;i++){//矩形數量
            int x1, x2, y1, y2;
            scanf("%d%d%d%d", &x1,&y1,&x2,&y2);//吃入矩形長寬
            int temp = 0;
            if(x1>x2){
                temp = x2;
                x2 = x1;
                x1 = temp;
            }
            if(y1>y2){
                temp = y2;
                y2 = y1;
                y1 = temp;
            }
            for(int x =x1; x<=x2;x++){
                for(int y = y1; y<=y2;y++){
                    broad[x-1][y-1] = true;
                }
            }
        }
        int epoint = 0;
        for(int i = 0; i<W;i++){
            for(int j = 0; j<H;j++){
                if(!broad[i][j]){
                    epoint++;
                }
            }
        }//掃一遍整個
        if(epoint == 0){
            printf("There is no empty spots.\n");
        }else if(epoint == 1){
            printf("There is one empty spot.\n");
        }else{
            printf("There are %d empty spots.\n", epoint);
        }
    }
    return 0;
}


/*
1.矩形座標讀取順序錯誤
    原本 scanf("%d%d%d%d", &x1,&x2,&y1,&y2);
    正確應該是 scanf("%d%d%d%d", &x1,&y1,&x2,&y2);
2.矩形迴圈範圍沒包含邊界
    你用了 < x2、< y2，導致最後一行/列沒被覆蓋。
    應該用 <= x2、<= y2。
3.座標系統沒有轉換
    題目座標是 1-based，陣列是 0-based。
    存取時要減 1，例如 broad[x-1][y-1] = true;
4.掃描棋盤時索引錯誤
    你用了 broad[i-1][j-1]，在 i=0 或 j=0 時會越界。
    應該直接用 broad[i][j]。
5.輸出文字小錯
    題目要求 There is one empty spot.
    你寫成 spots.，要改成單數。
*/
