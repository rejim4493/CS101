/*	讀入每筆案例→計算平均→統計成績 > 平均的人數→輸出百分比（小數三位並加上 %）*/
/*
It is said that 90% of frosh expect to be
above average in their class. You are to
provide a reality check.
Input
The first line of standard input contains
an integer C, the number of test cases.
C data sets follow. Each data set begins
with an integer, N, the number of people
in the class (1 ≤ N ≤ 1000). N integers
follow, separated by spaces or newlines,
each giving the final grade (an integer between 0 and 100) of a student in the class.
Output
For each case you are to output a line giving the percentage of students whose grade is above average,
rounded to 3 decimal places.
Sample Input
5
5 50 50 70 80 100
7 100 95 90 80 70 60 50
3 70 90 80
3 70 90 81
9 100 99 98 97 96 95 94 93 91
Sample Output
40.000%
57.143%
33.333%
66.667%
55.556%
*/

#include <stdio.h>

int main(void){
    int casenum = 0;
    scanf("%d", &casenum);//確認輸入幾筆case
    for(int i = 0; i < casenum; i+=1){
        int N = 0;//代表每班人數
        scanf("%d", &N);
        int a[1000] = {};//上限每班1000位學生
        float sum = 0; //成績加總
        for(int j = 0; j < N; j+=1){
            scanf("%d", &a[j]);
            sum += a[j];//總分
        }//讀入班級中每筆成績並加總
        float ave = sum/N;//代表平均，存入加總除以人數
        float pass = 0;//代表有過的人數
        for(int j = 0; j < N; j+=1){
            if(a[j]>ave){
                pass += 1;
            }
        }//若該筆成績高於平均則有過的人數+1
        printf("%.3f%%\n", (pass/N)*100);//有過的人數除以每班人數，乘以100為百分比
    }//case的loop
    return 0;
}
//以下為測試用的列印
//            printf("%d\n", a[j]);
//            printf("%f\n", sum);
//        printf("%f\n%f\n", pass, ave, N);
