/*
◼ You plan to deposit d dollars each year and aim to
reach g dollars. The annual interest rate is r.
◼ Write a program to calculate the number of years
required to reach your goal.

input
d = 1000, g = 10000, r = 50(%)
*/
#include <stdio.h>

int main(void){
  int d = 0,g = 0, r = 0;
      scanf("%d%d%d", &d, &g, &r);
      int y = 0, sum = 0;
      while(sum < g){
          sum = (sum + d) * (1+0.01*r);
          y++;
      }
      printf("We need %d years to reach the goal(%d)\n", y, g);
  
      return 0;
}

/*
為浮點數鋪成的題目，與double做計算得到的值也會是double
*/
